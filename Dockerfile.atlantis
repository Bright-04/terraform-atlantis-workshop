# Custom Atlantis image for workshop
FROM ghcr.io/runatlantis/atlantis:v0.27.0

# No need for Conftest - using alternative compliance validation approach
# The compliance validation is now handled by:
# 1. Terraform built-in validation (validation.tf)
# 2. Custom Atlantis workflow (atlantis.yaml)
# 3. PowerShell validation script (scripts/validate-compliance.ps1)

USER atlantis

# Set environment variables for the alternative approach
ENV ATLANTIS_ENABLE_POLICY_CHECKS=false
