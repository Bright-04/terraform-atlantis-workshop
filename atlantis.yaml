version: 3
projects:
    - name: terraform-atlantis-workshop
      dir: terraform
      terraform_version: v1.6.0
# Using default workflow for maximum reliability
# Compliance validation is now handled by Terraform-native validation in compliance-validation.tf
# This provides comprehensive validation without external dependencies or complex workflows
